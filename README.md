# dotfiles

Make sure to place `.zshenv` file in your home directory `~/.zshenv`.

move the other files to a zsh folder in your `.config` directory

```bash
ls -a ~/.config/zsh/
.  ..  .zcompdump  .zprofile  .zsh_aliases  .zshrc  plugins/
```


---
